{
  "name": "capture",
  "version": "1.0.0",
  "description": "App for capturing stuff",
  "main": "server.js",
  "scripts": {
    "build": "cd capture-ui && yarn run build && cd .. && cp -R capture-ui/build/ dist/src/web-app/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-fileupload": "^1.2.1",
    "http": "0.0.1-security",
    "multer": "^1.4.3",
    "request": "^2.88.2",
    "stream": "0.0.2",
    "xmldom": "^0.6.0"
  }
}
